Run on Windows:
- Double-click start.cmd  (recommended), or
- PowerShell: powershell -ExecutionPolicy Bypass -File .\start.ps1

If Python is missing, start script will install Python 3.11 via winget (with admin prompt), then set up venv, install deps, and run the server.
